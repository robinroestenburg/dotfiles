source "$HOME/.dotfiles/config/zsh/global.sh"
source "$HOME/.dotfiles/config/zsh/personal.sh"

# twk
alias twk="/Users/robin/Projects/automator-ops/bin/twk"

# prompt
setopt prompt_subst
PROMPT='$($HOME/.dotfiles/scripts/prompt $?)'

# mysql-client
#export PATH="$(brew --prefix mysql-client)/bin:$PATH"
# For compilers to find mysql-client you may need to set:
# export LDFLAGS="-L/opt/homebrew/opt/mysql-client/lib"
# export CPPFLAGS="-I/opt/homebrew/opt/mysql-client/include"

# homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"

# zoxide
eval "$(zoxide init zsh)"

# mise
eval "$(mise activate zsh)"

# docker cli completions
fpath=(/Users/robin/.docker/completions $fpath)
autoload -Uz compinit
compinit
