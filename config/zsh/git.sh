# Git aliases
# alias cdb='base=$(git rev-parse --show-cdup) && cd $base'
alias st='git status'
alias co='git checkout'
alias ready='git rebase -i'
alias merge='git ff'

alias f='git fa && git status'
alias fetch='git fa && git status'

alias cob='git branch | cut -c 3- | selecta | xargs git checkout'
alias cm='git commit'
alias amend='git commit --amend'
alias cm='git commit --message'
alias up='git up'
alias upstash='git stash && git pull --ff-only && git stash pop'
alias br='git branch'
alias branch='git branch'
alias lg='git log -p'
alias ll='git l'
alias la='git la'
alias aa='git add --all && git status -sb'
alias d='git diff'
alias df='git diff'
alias dc='git diff --cached'
alias push='git push'
